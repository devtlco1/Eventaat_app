# Blueprint location

The **official source of truth** for the Eventaat rebuild is:

- `docs/eventaat_blueprint_v1.md`

This root file is intentionally a short pointer to avoid having two competing blueprint copies.

